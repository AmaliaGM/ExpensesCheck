<div id="<%= dom_id category %>">
  <h2>
    <strong>Category:</strong>
    <%= category.name %>
  </h2>
  
  <% category.expenses.each do |expense| %>
    <p>
      <%= expense.title %> | <%= link_to "View Expense", expense %>
    </p>
  <% end %>
</div>
