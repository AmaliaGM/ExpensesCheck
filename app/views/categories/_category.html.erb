<nav class="navbar navbar-dark bg-primary">
<h2 class="navbar-brand" href="#">
<div class="container-fluid">
<%= category.icon %> <%= category.name %>
</h2>
  </div>
</nav>
<br>
<div class="hstack gap-3">
  <div class="bg-light border">Total Spent:</div>
  <div class="bg-light border ms-auto"><%= number_to_currency(category.expenses.sum(:price)) %>
  </div>
</div>
<br>
<% category.expenses.each do |expense| %>
<div class="hstack gap-3">
  <div class="bg-light border"><%= expense.title %></div>
  <div class="bg-light border ms-auto"><%= expense.price %></div>
  </div>
  </div>
  <%= link_to "Add expense", new_expense_path %>
  <% end %>
