<div id="<%= dom_id expense %>">
  <p>
  <div class="bg-light border"> <%= link_to expense.category.name, expense.category %>
  </div>
  <div class="hstack gap-3">
  <%= expense.title %>
  <div class="ms-auto"><%= expense.price %></div><br>
  </div>
</div>
